{% func SonarProps( sonarOrgKey string, sonarProjectKey string, sonarInclusions string, sonarExclusions string  ) %} {%s sonarOrgKey %}
sonar.projectKey={%s sonarProjectKey %}

# Define the same root directory for sources and tests (if any)
sonar.sources = .

{%s sonarInclusions %}

{%s sonarExclusions %}


{% endfunc %}