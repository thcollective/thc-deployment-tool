{% func SonarProps( sonarOrgKey string, sonarProjectKey string, sonarRootDir string, sonarInclusions string, sonarExclusions string  ) %} {%s sonarOrgKey %}
sonar.organization={%s sonarOrgKey %}

sonar.projectKey= {%s sonarProjectKey %}

# Define the same root directory for sources and tests (if any)
sonar.sources = {%s= sonarRootDir %}

{%s sonarInclusions %}

{%s sonarExclusions %}


{% endfunc %}